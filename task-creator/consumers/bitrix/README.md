# Bitrix24 Integration Module

Модуль интеграции с Bitrix24 для создания задач из процессов Camunda BPM.

## Описание

Этот модуль обрабатывает сообщения из RabbitMQ очереди `bitrix24.queue` и создает соответствующие задачи в Bitrix24 через REST API.

## Компоненты

- `handler.py` - Основной обработчик сообщений для создания задач в Bitrix24
- `config.py` - Конфигурация специфичная для Bitrix24
- `README.md` - Эта документация

## Конфигурация

### Переменные окружения для Bitrix24

```bash
# Обязательные настройки
BITRIX_WEBHOOK_URL=https://bx.eg-holding.ru/rest/1/123123123123

# Опциональные настройки
BITRIX_DEFAULT_RESPONSIBLE_ID=1
BITRIX_DEFAULT_PRIORITY=2
BITRIX_REQUEST_TIMEOUT=30
BITRIX_MAX_DESCRIPTION_LENGTH=10000
```

### Обязательные пользовательские поля в Bitrix24

Перед запуском сервиса необходимо создать следующие пользовательские поля для сущности **Задачи (TASKS_TASK)**:

| Поле | Тип | Описание |
|------|-----|----------|
| `UF_CAMUNDA_ID_EXTERNAL_TASK` | string | Уникальный идентификатор External Task из Camunda |
| `UF_RESULT_ANSWER` | enumeration | Ответ пользователя на вопрос задачи |
| `UF_RESULT_QUESTION` | string | Вопрос для задачи, требующей ответа |
| `UF_RESULT_EXPECTED` | boolean | Флаг, требуется ли ответ от пользователя |
| `UF_ELEMENT_ID` | string | ID элемента BPMN диаграммы (activityId) |
| `UF_PROCESS_INSTANCE_ID` | string | ID экземпляра процесса Camunda |

> **Важно**: Сервис проверяет наличие всех обязательных полей при старте. Если хотя бы одно поле отсутствует или имеет неверный тип — сервис не запустится с фатальной ошибкой.

## Поддерживаемые топики

- `bitrix_create_task` - Создание новой задачи
- `bitrix_update_task` - Обновление существующей задачи
- `bitrix_create_deal` - Создание новой сделки
- `bitrix_update_deal` - Обновление существующей сделки
- `bitrix_create_contact` - Создание нового контакта
- `bitrix_create_lead` - Создание нового лида
- `bitrix_update_lead` - Обновление существующего лида
- `bitrix_create_company` - Создание новой компании
- `bitrix_update_company` - Обновление существующей компании

## Формат входящих сообщений

Модуль обрабатывает сообщения в формате JSON из Camunda:

```json
{
  "task_id": "abc123",
  "topic": "bitrix_create_task",
  "variables": {
    "title": {"value": "Название задачи"},
    "description": {"value": "Описание задачи"},
    "responsible_id": {"value": "123"},
    "priority": {"value": "2"},
    "deadline": {"value": "2024-12-31 23:59:59"}
  },
  "metadata": {
    "inputParameters": {},
    "outputParameters": {},
    "extensionProperties": {}
  }
}
```

## Дополнительные переменные процесса

Для расширенного управления задачами Bitrix24 используйте следующие переменные Camunda (значения можно передавать как raw int или объект `{"value": 123}`):

- `parentTaskId` *(Integer)* — ID родительской задачи Bitrix24. При наличии все создаваемые задачи получают поля `PARENT_ID=parentTaskId` и `SUBORDINATE="Y"`, что делает их подзадачами указанного родителя.
- `groupId` *(Integer)* — ID рабочей группы/проекта Bitrix24. Используется как fallback, когда в шаблоне задачи не задан `GROUP_ID`.
- `diagramOwner` *(Integer)* — ID владельца диаграммы/процесса. Если в шаблоне отсутствуют наблюдатели, обработчик добавит этого пользователя в `AUDITORS`.

Эти переменные достаточно задать один раз в начале процесса Camunda — консьюмер Bitrix прочитает их из переменных процесса для каждого создаваемого задания.

## Извлечение данных

Обработчик извлекает данные из переменных сообщения по следующему приоритету:

### Заголовок задачи
- `title`, `name`, `subject`, `task_title`, `task_name`

### Ответственный
- `responsible_id`, `responsible`, `assignee_id`, `assignee`, `user_id`

### Приоритет
- `priority`, `task_priority`, `urgency`, `importance`
- Поддерживает: числовые значения (1-3) или текстовые ('low', 'normal', 'high')

### Дедлайн
- `deadline`, `due_date`, `end_date`, `task_deadline`

## Пользовательские поля (UF_)

Модуль поддерживает автоматическое извлечение и передачу пользовательских полей из `extensionProperties` в метаданных сообщения.

### Поддерживаемые пользовательские поля

#### UF_RESULT_EXPECTED
- **Тип**: Булево поле (Да/Нет)
- **Назначение**: Индикатор того, ожидается ли результат выполнения задачи
- **Формат в Camunda**: `"true"`, `"false"`, `"1"`, `"0"`, `"да"`, `"нет"`, `"yes"`, `"no"`
- **Формат в Bitrix24**: `"Y"` или `"N"`

#### UF_RESULT_QUESTION
- **Тип**: Строковое поле
- **Назначение**: Вопрос или описание ожидаемого результата
- **Формат**: Текстовая строка (обрезается до непустых значений)

#### UF_ELEMENT_ID
- **Тип**: Строковое поле
- **Назначение**: Сохраняет `elementId` из BPMN диаграммы Storm для каждой созданной задачи Bitrix24
- **Использование**: Позволяет искать существующие задачи по элементу диаграммы и строить связи «предшественник → последователь»
- **Формирование**: Заполняется автоматически при создании задач (как из шаблонов, так и в fallback-режиме)

#### UF_PROCESS_INSTANCE_ID
- **Тип**: Строковое поле
- **Назначение**: Сохраняет `processInstanceId` — уникальный идентификатор экземпляра процесса Camunda
- **Использование**: Позволяет различать задачи разных запусков одного и того же процесса. Используется совместно с `UF_ELEMENT_ID` для точного поиска предшественников
- **Формирование**: Заполняется автоматически из данных сообщения RabbitMQ при создании задач

### Предшественники задач

Для корректного определения задач-предшественников используется комбинация двух полей:
- `UF_ELEMENT_ID` — идентификатор элемента на BPMN-диаграмме
- `UF_PROCESS_INSTANCE_ID` — идентификатор экземпляра процесса

Это необходимо потому, что один и тот же `element_id` будет у всех задач одного типа во всех запусках процесса. Без `process_instance_id` поиск предшественника мог бы вернуть задачу из другого экземпляра процесса.

**Алгоритм работы:**

1. BitrixTaskHandler извлекает `diagramId`, `elementId` и `processInstanceId` из метаданных ExternalTask
2. По API `imena.camunda.diagram.responsible.get` запрашивается список `PREDECESSOR_IDS` для текущего элемента диаграммы
3. Для каждого элемента-предшественника выполняется поиск задачи Bitrix24 по комбинации `UF_ELEMENT_ID` + `UF_PROCESS_INSTANCE_ID`. Это гарантирует, что будет найдена задача именно из того же экземпляра процесса
4. Найденные задачи добавляются в поле `SE_PROJECTDEPENDENCE` c типом связи `TYPE = 2` (Finish-Start)
5. После успешного создания задачи дополнительно вызывается кастомный REST-метод `imena.camunda.task.dependency.add`, который принудительно создаёт связь `taskId → dependsOnId` (Finish-Start) даже если стандартное поле игнорируется
6. Если задача-предшественник ещё не создана, связь пропускается с предупреждением в логах
7. При создании задач по шаблону и в fallback-режиме поля `UF_ELEMENT_ID` и `UF_PROCESS_INSTANCE_ID` добавляются автоматически, поэтому повторные запуски обработчика корректно выстраивают цепочку зависимостей

> **Важно**: Оба поля (`UF_ELEMENT_ID` и `UF_PROCESS_INSTANCE_ID`) должны быть созданы в Bitrix24 перед запуском сервиса. Сервис проверяет наличие обязательных полей при старте и не запустится без них.

### Результаты предшествующих задач

При наличии задач-предшественников модуль автоматически:

1. **Получает результаты** завершённых предшествующих задач через API `tasks.task.result.list`
2. **Добавляет текст результатов** в описание создаваемой задачи в формате:

```
📋 Результаты предшествующих задач:

Задача №438:
  1. Какой-то текстовый результат для "1.7. Начисление заработной платы"
  2. 1.7. Начисление заработной платы - завершена

Задача №437:
  • 1.6. Начисление процентов по займам - завершена
     📎 Файлы: BITRIX-ADDON-REST-API.md
```

3. **Прикрепляет файлы** из результатов предшественников к новой задаче через `tasks.task.files.attach`

Это позволяет исполнителю новой задачи видеть контекст выполнения предыдущих этапов процесса и иметь доступ к приложенным документам без необходимости переходить в другие задачи.

### Анкеты задач (questionnaires)

- Если шаблон задачи Bitrix содержит секцию `questionnaires.items`, обработчик сохраняет её и после успешного `tasks.task.add` вызывает `imena.camunda.task.questionnaire.add` с этим же списком (v2.3 — без преобразований).
- Поддерживаются три сценария ответа `imena.camunda.tasktemplate.get`: есть анкеты, пустой список, или секция отсутствует — создание задач остаётся совместимым.
- Формат анкет соответствует `BITRIX-ADDON-REST-API-v.2.3.md`: обязательный `CODE` у анкеты и вопросов, типы `string|integer|boolean|date|enum|user`, `ENUM_OPTIONS` для `enum`, поля `total` и `has_codes` в корне `questionnaires`.
- При ошибке добавления анкет задача остаётся созданной; в логах появится предупреждение и тело ответа Bitrix24.

Пример рабочего ответа `imena.camunda.tasktemplate.get` (сокращённо):
```
"questionnaires": {
  "items": [
    {
      "CODE": "PERSONAL",
      "TITLE": "Личные данные",
      "questions": [
        {"CODE": "TESTOVYY", "TYPE": "string", "IS_REQUIRED": "Y"},
        {"CODE": "NUZHEN_NOUT", "TYPE": "boolean", "IS_REQUIRED": "Y"},
        {"CODE": "TEST_VYBOR_IZ_SPISKA", "TYPE": "enum", "ENUM_OPTIONS": ["Вариант 1","Вариант 2"]},
        {"CODE": "DATA_VYHODA", "TYPE": "date"},
        {"CODE": "RUKOVODITEL", "TYPE": "user"}
      ]
    }
  ],
  "total": 1,
  "has_codes": true
}
```

## Обязательный результат задачи (SE_PARAMETER)

Для всех создаваемых задач автоматически устанавливается параметр **"Не завершать задачу без результата"**:

```python
SE_PARAMETER: [{'CODE': 3, 'VALUE': 'Y'}]
```

| Параметр | Значение |
|----------|----------|
| **Таблица БД** | `b_tasks_task_parameter` |
| **Константа** | `ParameterTable::PARAM_RESULT_REQUIRED = 3` |
| **Эффект** | При попытке закрыть задачу пользователь обязан ввести результат |

Это гарантирует, что задачи из Camunda-процессов не будут закрыты без явного указания результата выполнения, что важно для отслеживания хода бизнес-процессов.

> **Примечание**: Поле `SE_PARAMETER` не указано в официальной документации Bitrix24 API, но успешно работает при создании и обновлении задач.

### Пример сообщения с пользовательскими полями

```json
{
  "task_id": "2d0bf3ab-5e83-11f0-a3a6-00b436387543",
  "topic": "bitrix_create_task",
  "variables": {
    "projectManagerId": "3",
    "projectId": "2"
  },
  "metadata": {
    "extensionProperties": {
      "UF_RESULT_EXPECTED": "true",
      "UF_RESULT_QUESTION": "снос объектов по уведомлению требуется?",
      "assigneeId": "15297786"
    },
    "activityInfo": {
      "name": "Выяснить: снос объектов по уведомлению требуется?"
    }
  }
}
```

### Обработка пользовательских полей

1. Модуль автоматически извлекает все поддерживаемые пользовательские поля из `metadata.extensionProperties`
2. Выполняет преобразование типов данных согласно требованиям Bitrix24 API
3. Добавляет поля в запрос создания задачи только если они присутствуют в исходном сообщении
4. Логирует информацию о найденных и обработанных пользовательских полях

### Расширение поддержки

Для добавления поддержки новых пользовательских полей:

1. Добавьте имя поля в список `supported_user_fields` в методе `_extract_user_fields`
2. При необходимости добавьте специальную обработку типа данных
3. Убедитесь, что поле создано в настройках Bitrix24 для сущности "Задачи" (TASKS_TASK)

## Статистика

Модуль ведет статистику обработки:

### Основные метрики
- Общее количество сообщений
- Успешно созданные задачи
- Ошибки при создании
- Время работы
- Процент успеха
- Сообщений в минуту

### Шаблоны
- Запрошено шаблонов
- Найдено / не найдено шаблонов
- Ошибки API шаблонов

### Файлы шаблонов
- Найдено файлов в шаблонах
- Успешно прикреплено
- Ошибки прикрепления

### Зависимости задач
- Попыток создания зависимостей
- Успешно создано
- Ошибок создания

### Результаты предшественников
- Получено результатов предшественников
- Ошибок получения результатов
- Прикреплено файлов от предшественников
- Ошибок прикрепления файлов предшественников

## Используемые API методы Bitrix24

### Стандартные REST API
| Метод | Назначение |
|-------|------------|
| `tasks.task.add` | Создание новой задачи |
| `tasks.task.list` | Поиск задач (по `UF_ELEMENT_ID` + `UF_PROCESS_INSTANCE_ID`) |
| `tasks.task.files.attach` | Прикрепление файлов к задаче |
| `tasks.task.result.list` | Получение результатов задачи |
| `task.commentitem.get` | Получение деталей комментария (для файлов результатов) |
| `task.checklistitem.add` | Создание элементов чек-листа |

### Кастомные REST API (imena.*)
| Метод | Назначение |
|-------|------------|
| `imena.camunda.tasktemplate.get` | Получение шаблона задачи по processId/elementId |
| `imena.camunda.diagram.responsible.get` | Получение ответственного и предшественников элемента |
| `imena.camunda.task.dependency.add` | Создание зависимости между задачами |
| `imena.camunda.sync` | Синхронизация после создания задачи |
| `imena.camunda.task.questionnaire.add` | Добавление анкет из шаблона в созданную задачу |

## Логирование

Используется библиотека `loguru` для структурированного логирования всех операций.

## Тестирование

Для тестирования интеграции используйте тестовые скрипты из корневой папки проекта. 