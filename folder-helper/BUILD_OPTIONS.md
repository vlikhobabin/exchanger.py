# Варианты сборки Windows EXE на Linux

## 🎯 Вариант 1: GitHub Actions (РЕКОМЕНДУЕТСЯ) ⭐

**Самый простой и надежный способ!**

### Как использовать:

1. **Создайте GitHub репозиторий** (если еще нет):
   ```bash
   cd /opt/exchanger.py
   git init
   git remote add origin https://github.com/ваш-username/exchanger.py.git
   ```

2. **Добавьте файлы в Git**:
   ```bash
   git add bpmn/folder-helper/
   git add .github/workflows/build-windows-exe.yml
   git commit -m "Add folder-helper and Windows build workflow"
   git push origin main
   ```

3. **Запустите сборку**:
   - Перейдите на GitHub: `https://github.com/ваш-username/exchanger.py/actions`
   - Нажмите "Build Windows EXE" → "Run workflow"
   - Дождитесь завершения (2-3 минуты)

4. **Скачайте готовый EXE**:
   - В разделе "Artifacts" скачайте `folder-helper-windows`
   - Распакуйте архив - внутри будет `folder-helper.exe`

### Преимущества:
- ✅ Не требует установки Python на Windows
- ✅ Автоматическая сборка при изменениях
- ✅ Бесплатно для публичных репозиториев
- ✅ Работает прямо с Linux сервера

---

## 🔧 Вариант 2: Wine + PyInstaller (Экспериментальный)

**Сложнее, но работает локально**

### Установка Wine:

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install wine wine64 wine32

# Или установите Wine из официального репозитория
```

### Установка Python в Wine:

```bash
# Скачайте Python installer для Windows
wget https://www.python.org/ftp/python/3.12.0/python-3.12.0-amd64.exe

# Установите через Wine
wine python-3.12.0-amd64.exe

# Установите зависимости
wine pip install flask pyinstaller
```

### Сборка:

```bash
cd /opt/exchanger.py/bpmn/folder-helper
wine pyinstaller --noconsole --onefile --name folder-helper \
  --hidden-import flask --hidden-import tkinter \
  folder-helper.py
```

### Недостатки:
- ⚠️ Сложная настройка
- ⚠️ Может работать нестабильно
- ⚠️ Требует много места (Wine + Python)
- ⚠️ Медленнее чем нативная сборка

---

## 💻 Вариант 3: Локальная Windows машина

**Самый надежный, но требует Windows**

1. Скопируйте файлы на Windows:
   - `folder-helper.py`
   - `build-windows.bat`

2. Запустите `build-windows.bat`

3. Готовый `folder-helper.exe` будет в `dist\`

---

## ☁️ Вариант 4: Облачные Windows сервисы

### Azure DevOps
- Бесплатный план: 1800 минут/месяц
- Windows агенты доступны

### AppVeyor
- Бесплатно для открытых проектов
- Автоматическая сборка Windows приложений

### GitHub Actions (уже настроен)
- ✅ Самый простой вариант
- ✅ Бесплатно для публичных репозиториев
- ✅ 2000 минут/месяц для приватных (бесплатно)

---

## 📊 Сравнение вариантов

| Вариант | Сложность | Надежность | Скорость | Стоимость |
|---------|-----------|------------|----------|-----------|
| GitHub Actions | ⭐ Легко | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Бесплатно |
| Wine | ⭐⭐⭐ Сложно | ⭐⭐⭐ | ⭐⭐ | Бесплатно |
| Локальный Windows | ⭐⭐ Средне | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Требует Windows |
| Azure DevOps | ⭐⭐ Средне | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Бесплатно (лимиты) |

---

## 🚀 Рекомендация

**Используйте GitHub Actions** - это самый простой и надежный способ собрать Windows EXE прямо с Linux сервера без установки дополнительного ПО.

