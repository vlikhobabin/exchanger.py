# 🚀 Быстрый старт: Сборка Windows EXE на Linux

## ✅ РЕШЕНИЕ: GitHub Actions (без установки Python на Windows!)

Вам **НЕ НУЖНО** устанавливать Python на Windows! Можно собрать прямо с Linux сервера через GitHub Actions.

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка Git репозитория

```bash
cd /opt/exchanger.py

# Если репозиторий еще не инициализирован
git init

# Если репозиторий уже есть, просто добавьте файлы
git add bpmn/folder-helper/
git add .github/workflows/build-windows-exe.yml
git commit -m "Add folder-helper Windows build"
```

### Шаг 2: Создайте GitHub репозиторий

1. Перейдите на [github.com](https://github.com)
2. Создайте новый репозиторий (можно приватный)
3. Скопируйте URL репозитория

### Шаг 3: Загрузите код на GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Загрузите код
git push -u origin main
# или
git push -u origin master
```

### Шаг 4: Запустите сборку

1. Откройте ваш репозиторий на GitHub
2. Перейдите в раздел **"Actions"**
3. Выберите workflow **"Build Windows EXE"**
4. Нажмите **"Run workflow"** → **"Run workflow"**
5. Дождитесь завершения (2-3 минуты)

### Шаг 5: Скачайте готовый EXE

1. После завершения сборки нажмите на выполненный workflow
2. Прокрутите вниз до раздела **"Artifacts"**
3. Скачайте `folder-helper-windows`
4. Распакуйте ZIP архив
5. Внутри будет готовый `folder-helper.exe` ✅

---

## 🎯 Альтернатива: Если нет GitHub

### Вариант A: Попросите коллегу

Передайте файлы коллеге с Windows:
- `folder-helper.py`
- `build-windows.bat`

Он запустит `build-windows.bat` и отправит вам готовый `.exe`

### Вариант B: Wine (сложнее)

См. подробную инструкцию в `BUILD_OPTIONS.md`

---

## ❓ FAQ

**Q: Нужен ли мне Python на Windows?**  
A: Нет! GitHub Actions соберет exe автоматически в облаке.

**Q: Это бесплатно?**  
A: Да, для публичных репозиториев - полностью бесплатно. Для приватных - 2000 минут/месяц бесплатно.

**Q: Сколько времени занимает сборка?**  
A: Около 2-3 минут.

**Q: Можно ли автоматизировать?**  
A: Да! Workflow настроен на автоматический запуск при изменении файлов в `bpmn/folder-helper/`

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи в GitHub Actions
2. Убедитесь, что все файлы загружены в репозиторий
3. Проверьте, что workflow файл находится в `.github/workflows/`

